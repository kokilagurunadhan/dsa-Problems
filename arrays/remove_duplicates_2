class Solution(object):
    def removeDuplicates(self, nums):
        
        s=0
        for f in range(len(nums)):
            if(s<2 or nums[f]!=nums[s-2]):
                nums[s]=nums[f]
                s+=1
            
        return s