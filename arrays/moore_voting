class Solution:
    def majorityElement(self, nums):
        candidate=0
        count=0
        for i in nums:
            if(count==0):
                candidate=i
                count=1
            elif(candidate!=i):
                count-=1
            else:
                count+=1
        return candidate
nums=[2,2,2,3,3,2]
s=Solution()
print(s.majorityElement)